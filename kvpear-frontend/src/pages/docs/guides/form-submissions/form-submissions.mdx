import Highlight from 'react-highlight'
import { Spacer } from '../../../../components/common/Mdx/Mdx';


# Capturing form submissions using KV Pear
<Spacer height={2} />



### How we do it

We capture form submissions on our own [contact](/contact) form using KV Pear. We do this by using a simple form event listener in JavaScript.

Given our HTML form:

<Highlight className="HTML block">
</Highlight>

<Highlight className="JavaScript block">
const onSubmit = (e) => (
  
)
</Highlight>